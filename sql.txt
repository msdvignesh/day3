CREATE TABLE EMP2(
EMPNO       INT         CONSTRAINT EMP_PK PRIMARY KEY,
ENAME       VARCHAR(20),
GENDER      CHAR(1)     CONSTRAINT EMP_GENDER DEFAULT 'F',
JOB         VARCHAR(10) CONSTRAINT EMP_JOB CHECK (JOB IN('SALESMAN','MANAGER','CLERK','ANALYST','PRESIDENT')),
MGR         INT         CONSTRAINT EMP_MGR  REFERENCES EMP2(EMPNO),
HIREDATE    DATE        CONSTRAINT EMP_HIRE_DATE CHECK (HIREDATE>'2022-04-01'),
SAL         INT         CONSTRAINT EMP_SAL CHECK (SAL>300),
COMM        INT         CONSTRAINT EMP_COMM  CHECK (COMM>200),
DEPTNO      INT         CONSTRAINT EMP_DEPTNO REFERENCES DEPT(DEPTNO)
);


CREATE TABLE EMP3(
EMPNO       INT         CONSTRAINT EMP_PK PRIMARY KEY,
ENAME       VARCHAR(20),
GENDER      CHAR(1)  DEFAULT 'F',
JOB         VARCHAR(10) CONSTRAINT EMP_JOB CHECK (JOB IN('SALESMAN','MANAGER','CLERK','ANALYST','PRESIDENT')),
MGR         INT         CONSTRAINT EMP_MGR  REFERENCES EMP2(EMPNO),
HIREDATE    DATE        CONSTRAINT EMP_HIRE_DATE CHECK (HIREDATE>'2022-04-01'),
SAL         INT         CONSTRAINT EMP_SAL CHECK (SAL>300),
COMM        INT         CONSTRAINT EMP_COMM  CHECK (COMM>200),
DEPTNO      INT         CONSTRAINT EMP_DEPTNO REFERENCES DEPT(DEPTNO)
);

INSERT INTO EMP3 VALUES (1001,'EMPLOYEE 1','M','MANAGER',NULL,'1981-03-01',1500,300,NULL);


CREATE TABLE EMP4 (
 EMPNO               int CONSTRAINT EMP_PK  PRIMARY KEY,
 ENAME               VARCHAR(10) NOT NULL,
 GENDER		     CHAR(1) DEFAULT 'F', 
 EMAIL		     VARCHAR(20) CONSTRAINT EMP_EMAIL UNIQUE,
 JOB                 VARCHAR(9) CONSTRAINT EMP_JOB  CHECK (JOB IN ('SALESMAN','CLERK','MANAGER','PRESIDENT','ANALYST')),
 MGR                 int REFERENCES CONSTRAINT EMP_MGR EMP2(EMPNO),
 HIREDATE            DATE CONSTRAINT EMP_HIRE_DATE CHECK (HIREDATE > '2021-03-31'),
 SAL                 int CONSTRAINT EMP_SAL CHECK (SAL>COMM),
 COMM                int  CONSTRAINT EMP_COMM CHECK (COMM > 200),

 DEPTNO              int CONSTRAINT EMP_DEPTNO REFERENCES DEPT(DEPTNO)  );




CREATE TABLE EMP5(
 EMPNO              int CONSTRAINT EMP5_PK  PRIMARY KEY,
 ENAME               VARCHAR(10) NOT NULL,
 GENDER            CHAR(1) DEFAULT 'F', 
 EMAIL                 VARCHAR(20) CONSTRAINT EMP5_EMAIL UNIQUE,
 JOB                    VARCHAR(9) CONSTRAINT EMP5_JOB  CHECK (JOB IN ('SALESMAN','CLERK','MANAGER','PRESIDENT','ANALYST')),
 MGR                   int  CONSTRAINT EMP5_MGR REFERENCES EMP5(EMPNO),
 HIREDATE          DATE CONSTRAINT EMP5_HIRE_DATE CHECK (HIREDATE > '2021-03-31'),
 SAL                      int CONSTRAINT EMP5_SAL CHECK (SAL>COMM),
 COMM                 int  CONSTRAINT EMP5_COMM CHECK (COMM > 200),

 DEPTNO             int CONSTRAINT EMP5_DEPTNO REFERENCES DEPT(DEPTNO)  );

CREATE TABLE EMP2(
EMPNO       INT         PRIMARY KEY,
ENAME       VARCHAR(20),
GENDER      CHAR(1)     DEFAULT 'F',
JOB         VARCHAR(10) CHECK (JOB IN('SALESMAN','MANAGER','CLERK','ANALYST','PRESIDENT')),
MGR         INT         REFERENCES EMP2(EMPNO),
HIREDATE    DATE        CHECK (HIREDATE>'2022-04-01'),
SAL         INT         CHECK (SAL>300),
COMM        INT         CHECK (COMM>200),
DEPTNO      INT         REFERENCES DEPT(DEPTNO)
);

CREATE TABLE EMP2(
EMPNO       INT         PRIMARY KEY,
ENAME       VARCHAR(20),
GENDER      CHAR(1)     DEFAULT 'F',
EMAIL       VARCHAR(20) NOT NULL,
JOB         VARCHAR(10) CHECK (JOB IN('SALESMAN','MANAGER','CLERK','ANALYST','PRESIDENT')),
MGR         INT         REFERENCES EMP2(EMPNO),
HIREDATE    DATE        CHECK (HIREDATE>'2022-03-30'),
SAL         INT         CHECK (SAL>300),
COMM        INT         CHECK (COMM>200),
DEPTNO      INT         REFERENCES DEPT(DEPTNO)
);

